# career-campus-two

This repository contains the restored mobile app source for the Career Campus APK build.

The current commit restores files from `APKbuild/mobile-artifacts` into `artifacts/mobile` and includes the APK build workflow configuration.

## Notes

- `artifacts/mobile` now matches the working apk build snapshot from `APKbuild/mobile-artifacts`
- `eas-build.yml` and relevant workflow files are included
